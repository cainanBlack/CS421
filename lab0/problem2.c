# include <stdio.h>

int main(){

	int width = 3;
	int height = 5;


	int perimeter = (2*width)+(2*height);
	int area = width*height;

	printf("Perimeter of the rectangle = %d inches.\nArea of the rectange = %d square inches.\n", perimeter, area);
	return 0;
}
